# 任务操作流程手册

> 面向 ALOHA2 + XENSE 触觉正式实验采集。本文档描述每个任务在遥操作采集时的物体摆放、操作流程、成功标准、失败标准和采集注意事项。

---

## 1. 通用采集规范

### 1.1 采集顺序

正式采集不要一开始直接录满 50 条。每个任务先采集 `5 episodes` 试采，质检通过后再补到 20 条，最后补到 50 条。

推荐第一轮任务顺序：

1. `aloha_flip_switch`
2. `aloha_hidden_property_grasp`
3. `aloha_slip_hold_or_pull`
4. `aloha_peg_insertion`

第二轮或时间允许时补充：

1. `aloha_pressure_wipe`
2. `aloha_towel_unfold`

### 1.2 传感器配置

所有任务默认使用：

- 4 路 RealSense 相机：`cam_high`, `cam_low`, `cam_left_wrist`, `cam_right_wrist`
- 4 个 XENSE G1-WS 触觉传感器
- XENSE FULL 模式，单个传感器输出 `(17, 120, 160)` 触觉数据

采集时只录 FULL 数据。后续 `visual_only`, `rectify`, `force`, `force_all`, `depth`, `marker2d`, `full` 等消融都从同一批 FULL 数据中选择不同输入通道，不需要为不同消融重新采集数据。

### 1.3 每条 Episode 开始前检查

开始录制前确认：

- 任务物体在预设初始区域内。
- 目标区域、容器、孔位或擦拭区域在相机视野内。
- 两个从臂夹爪不会在初始姿态遮挡关键视觉目标。
- 四个触觉传感器表面干净，无明显灰尘、油污或异物。
- 触觉传感器没有明显持续饱和、黑屏或异常噪声。
- 机械臂运动范围内没有无关物体。

### 1.4 每条 Episode 结束后记录

每条 episode 结束后建议记录：

- 成功或失败。
- 失败原因，例如抓空、滑落、插偏、压力过大、相机遮挡、物体初始位置异常。
- 是否出现传感器异常，例如某一路相机断帧、某个 XENSE 无数据。
- 是否需要废弃该 episode。

### 1.5 质量检查

每个任务完成 `5 episodes` 试采后，先运行数据检查脚本：

```bash
python examples/aloha2/check_collection_batch.py \
  --task aloha_flip_switch \
  --min_episodes 5
```

将 `--task` 替换为对应任务名。检查通过后再继续补录。

---

## 2. `aloha_flip_switch`：拨动开关

### 2.1 任务目标

机械臂接近大型拨杆开关，用夹爪或指尖稳定接触拨杆，将开关从初始档位拨到目标档位。

该任务是视觉主导的负对照任务，用于验证触觉并非在所有任务中都必要。预期 `visual_only` 与 `full` 的性能接近。

### 2.2 实验物品与初始摆放

- 使用一个固定在桌面上的大型拨杆开关或类似刚性开关装置。
- 开关底座应固定，避免拨动时整体滑动。
- 开关初始状态可以是向上、向下、向左或向右，但单批采集中应保持任务目标清晰。
- 开关应位于 `cam_high` 和至少一个腕部相机可见区域内。
- 目标档位最好有明显视觉状态，便于后续人工评估成功率。

### 2.3 单条 Episode 操作流程

1. 将主臂移动到安全初始姿态，从臂夹爪位于开关附近但不接触。
2. 观察高位相机和腕部相机，确认拨杆位置和目标方向。
3. 控制从臂缓慢接近拨杆，夹爪尖端或夹爪侧面与拨杆建立稳定接触。
4. 沿目标方向推动或拨动拨杆，避免撞到底座或桌面。
5. 当拨杆越过中间位置后，继续轻推直到目标档位稳定。
6. 保持短暂停顿，确认开关没有回弹。
7. 移开夹爪，结束 episode。

### 2.4 成功标准

- 开关最终到达目标档位。
- 开关状态稳定，没有明显回弹到原档位。
- 机械臂没有碰倒或移动开关底座。
- episode 结束时目标状态可由相机判断。

### 2.5 失败标准

- 夹爪没有接触到拨杆。
- 拨杆只被轻微碰动，但没有到达目标档位。
- 拨动方向错误。
- 开关底座被推倒或大幅移动。
- 机械臂或夹爪严重遮挡相机，导致无法判断最终状态。

### 2.6 随机化设计

- 初始档位可在小范围内变化，例如上拨到下、下拨到上。
- 开关在桌面上的位置可以小幅平移，但不要超出相机稳定可见区域。
- 接近方向可以略有变化，避免策略只记忆单一路径。

### 2.7 触觉机制关注点

- 该任务主要依赖视觉定位和刚性接触。
- Rectify 或 full tactile 可能只在确认接触瞬间有轻微帮助。
- 机制分析重点是证明 `visual_only ≈ full`，说明触觉价值具有任务依赖性。

### 2.8 采集注意事项

- 不要让开关底座自由滑动，否则任务会混入力控和推物体因素。
- 不要使用太小或太软的开关，否则触觉必要性会被人为放大。
- 每条 episode 的终点要留出足够帧数，让最终档位清晰可见。

---

## 3. `aloha_hidden_property_grasp`：隐藏属性抓取

### 3.1 任务目标

机械臂抓起外观相同但重量、摩擦或软硬程度不同的物体，将其搬运到目标容器中，并在整个搬运过程中保持不掉落。

该任务是第一阶段的核心触觉必要性任务。视觉无法可靠区分隐藏物理属性，策略需要依赖力和滑移反馈调整夹持。

### 3.2 实验物品与初始摆放

- 准备 3 到 4 类外观尽量一致的物体。
- 物体差异应主要体现在隐藏属性：
  - 重量不同，例如轻块和重块。
  - 表面摩擦不同，例如光滑外壳和高摩擦外壳。
  - 软硬不同，例如硬块和软垫块。
- 物体颜色、大小、外形尽量一致，避免视觉直接识别属性。
- 桌面上设置一个固定抓取区域和一个目标容器。
- 每条 episode 只放置一个待抓取物体，或清楚指定当前目标物体。

### 3.2.1 四类物体隐藏属性设计

建议使用 **一个基准物 + 三个单属性变化物**。这样可以把机制解释拆清楚：某一类和基准物相比只改变一个隐藏属性，避免重量、摩擦、软硬同时变化导致后续无法归因。

| 类别 | 名称 | 重量 | 表面摩擦 | 软硬 | 科学作用 |
| --- | --- | --- | --- | --- | --- |
| A | 基准物 | 轻 | 高摩擦 | 硬 | baseline |
| B | 重物 | 重 | 高摩擦 | 硬 | 只测试重量差异 |
| C | 易滑物 | 轻 | 低摩擦 | 硬 | 只测试摩擦差异 |
| D | 软物 | 轻 | 高摩擦 | 软 | 只测试软硬差异 |

核心成对比较关系：

- `A vs B`：只改变重量，保持外观、尺寸、颜色、表面材料和软硬程度尽量一致。该比较用于分析 `ForceResultant` 和 `ForceNorm` 是否能解释重物抬起后的总力变化和夹持需求。
- `A vs C`：只改变表面摩擦，保持重量、尺寸、颜色和软硬程度尽量一致。该比较用于分析 `Marker2D` 是否能提前反映滑移，以及模型是否会根据滑移趋势调整夹持。
- `A vs D`：只改变软硬或可压缩性，保持重量、尺寸、颜色和表面摩擦尽量一致。该比较用于分析 `ForceNorm` / `Depth` 是否能解释接触压力分布和软物体形变。

不建议把以下组合当作主要机制解释对象：

- `B vs C`
- `B vs D`
- `C vs D`

这些组合会同时改变多个隐藏属性，适合做整体难度观察，但不适合作为“哪个触觉状态为什么有用”的主要因果比较。

实际制作建议：

- 4 类物体的外观、尺寸、颜色、轮廓和抓取区域尽量一致。
- 轻物建议约 `50-80g`，重物建议约 `180-250g`。
- 物体尺寸建议约 `4-6 cm`，便于 ALOHA 夹爪稳定抓取。
- 低摩擦物体可以在相同外壳上贴光滑胶带、塑料膜或 PTFE 类材料；高摩擦物体可以使用橡胶、硅胶或防滑贴。
- 软物可以在相同外壳中加入泡棉、软硅胶或可压缩材料，但外观不要明显暴露内部结构。
- 50 条 episode 中 4 类物体尽量均衡，每类约 `12-13` 条。

### 3.3 单条 Episode 操作流程

1. 随机选择一种隐藏属性物体，放在抓取区域内。
2. 将目标容器放在固定位置，确认容器在 `cam_high` 可见。
3. 控制夹爪从安全姿态接近物体。
4. 夹爪接触物体两侧后逐步闭合，不要一开始用最大夹持力。
5. 轻微抬起物体，观察是否发生滑移或倾斜。
6. 如果物体有滑移趋势，继续增加夹持力或调整夹持位置。
7. 稳定抬起物体，移动到目标容器上方。
8. 降低高度，将物体放入容器。
9. 打开夹爪并撤离，确认物体留在容器中。

### 3.4 成功标准

- 物体被成功抓起。
- 搬运过程中物体没有掉落。
- 物体最终进入目标容器。
- 对软物体没有造成明显过度挤压或不可接受变形。

### 3.5 失败标准

- 抓空或只碰到物体但没有抓起。
- 物体在抬起或搬运过程中滑落。
- 夹持过紧导致软物体明显变形，且影响任务完成。
- 物体没有放入容器，或放置时弹出容器。
- 物体初始摆放导致任务不可执行。

### 3.6 随机化设计

- 每条 episode 随机选择重量、摩擦或软硬属性。
- 物体初始位置在抓取区域内小范围随机变化。
- 物体朝向可小幅随机变化，但不要让外观差异暴露隐藏属性。
- 操作者不要按固定顺序采集不同属性，避免数据中出现顺序偏差。

### 3.7 触觉机制关注点

- `force_all` 应帮助判断整体抓取稳定性和重物负载。
- `ForceResultant` 可用于解释物体抬起后总力变化。
- `ForceNorm` 可用于解释夹持正压力是否足够。
- `Marker2D` 可用于解释物体开始滑移的时刻和方向。
- 机制分析重点是比较 `visual_only/rectify` 与 `force_all/full` 的差距。

### 3.8 采集注意事项

- 外观一致性非常重要，否则任务会退化成视觉分类任务。
- 隐藏属性差异不能太小，否则 50 条数据可能难以体现差异。
- 也不能差异过大到操作者动作完全不同，否则会引入明显示教偏差。
- 采集时保持目标容器位置稳定，避免额外视觉随机性。

---

## 4. `aloha_slip_hold_or_pull`：滑移保持或拉动

### 4.1 任务目标

机械臂夹住布条、纸片、橡胶片或小片状物体，在可控滑移条件下保持物体不掉落，或沿指定方向拉动到目标距离。

该任务直接验证 Marker2D 对切向位移和滑移检测的作用，比完整布料展开更容易归因。

### 4.2 实验物品与初始摆放

- 可选物体：
  - 布条
  - 纸片
  - 橡胶片
  - 薄塑料片
  - 小型片状物体
- 物体一端可以固定在桌面、夹具或由另一只夹爪轻轻持有。
- 另一端作为目标夹持区域，供主操作夹爪抓取。
- 桌面上设置目标线或目标区域，用于判断拉动距离。
- 初始外露长度应足够产生轻微滑移，但不能太长导致运动不可控。

### 4.3 单条 Episode 操作流程

1. 将片状物体放到预设位置，确认可夹持端清晰可见。
2. 控制夹爪接近物体可夹持区域。
3. 轻轻闭合夹爪，建立接触并形成初始夹持。
4. 缓慢抬起或沿指定方向拉动，使物体产生可控张力。
5. 如果检测到滑移趋势，通过增加夹持力、调整姿态或减小拉动速度保持稳定。
6. 对保持任务：维持物体悬空或受拉状态到 episode 结束。
7. 对拉动任务：将物体拉到目标线或目标距离后停止。
8. 保持短暂停顿，确认物体没有继续滑落。

### 4.4 成功标准

- 物体在 episode 结束前没有从夹爪中脱落。
- 如果是拉动版本，物体达到指定目标距离或目标区域。
- 拉动方向大体正确，没有明显横向偏离。
- 物体没有被夹爪撕裂或严重损坏。

### 4.5 失败标准

- 物体从夹爪中滑落。
- 夹持过紧导致完全无法滑动，任务变成纯刚性拖拽。
- 拉动方向明显偏离目标方向。
- 物体被撕裂、折断或卡住。
- 另一端固定不稳定，导致整个任务目标变化。

### 4.6 随机化设计

- 材料类型可以在布、纸、橡胶之间变化。
- 初始夹持位置小范围变化。
- 初始外露长度小范围变化。
- 拉动目标距离小范围变化，但每个 episode 的目标应可被清楚判断。

### 4.7 触觉机制关注点

- `Marker2D` 是主要通道，用于检测切向位移和滑移方向。
- `Force` 辅助判断夹持力度是否足够。
- 机制分析重点是观察滑移发生前后 Marker2D 是否出现可解释变化。
- 预期 `marker2d/full` 优于 `rectify`，因为 image-only tactile 需要间接估计滑移。

### 4.8 采集注意事项

- 不要把任务做得太像简单拖拽物体，否则滑移检测价值会下降。
- 不要让物体一开始就夹得过紧，否则不会产生有研究价值的滑移信号。
- 保持滑移是可控的：应该允许轻微滑移，但不能频繁直接掉落。

---

## 5. `aloha_peg_insertion`：轴孔插入

### 5.1 任务目标

机械臂抓住 USB 插头、销钉或类似刚性插入物，移动到孔位附近，通过接触后的微调完成插入。

该任务是精细触觉 benchmark，重点验证滑移、接触力和卡滞信号对插入成功率的影响。

### 5.2 实验物品与初始摆放

- 使用 USB 插头、圆柱销钉、方形 peg 或其他可重复插入的刚性物体。
- 孔位装置应固定在桌面上，避免插入时整体移动。
- 孔位尺寸应有一定公差要求，不能太松，否则视觉即可完成；也不能太紧，否则示教成功率过低。
- 插入物初始位置放在固定抓取区域。
- 孔位应在 `cam_high` 和腕部相机中可见。

### 5.3 单条 Episode 操作流程

1. 将插入物放在抓取区域，将孔位装置固定到目标位置。
2. 控制夹爪接近插入物并稳定抓取。
3. 抬起插入物，调整姿态，使插入端朝向孔位。
4. 移动到孔位附近，降低速度。
5. 让插入物轻微接触孔口或导向边缘。
6. 根据接触反馈进行微小位置和姿态调整。
7. 如果出现卡滞或侧向力过大，略微退出并重新对准。
8. 沿孔轴方向缓慢推进，直到达到指定插入深度。
9. 保持短暂停顿，确认插入物稳定停留在孔中。

### 5.4 成功标准

- 插入物进入孔位。
- 插入深度达到预设标准，例如超过一半长度或达到机械限位。
- 孔位装置没有被明显推离原位置。
- 任务结束时插入状态可由相机判断。

### 5.5 失败标准

- 未能抓起插入物。
- 插入物没有接近孔位或找不到孔。
- 插入方向明显错误。
- 插入物卡在孔口且无法推进。
- 用力过大导致孔位装置移动、倾倒或损坏。

### 5.6 随机化设计

- 插入物初始位置小范围变化。
- 插入物初始朝向小范围变化。
- 孔位位置可小范围平移，但必须保持相机可见且任务可完成。
- 不建议在早期采集中大幅随机化孔位姿态，以免示教方差过高。

### 5.7 触觉机制关注点

- `Marker2D` 可反映接触后的微滑移、错位和局部切向运动。
- `Force` 可反映卡滞、侧向挤压和接触力不对称。
- 机制分析重点是插入失败前是否出现可解释的滑移或力异常。
- 预期 `marker2d + force` 或 `full` 在精细对准阶段优于 `rectify`。

### 5.8 采集注意事项

- 孔位固定必须可靠，否则任务会变成推物体。
- 插入物和孔位之间要有适度难度。
- 示教时动作要慢，尤其是接触后的微调阶段。
- 避免强行插入；遇到卡滞时应退出、重新对准、再推进。

---

## 6. `aloha_pressure_wipe`：固定区域短距离擦拭

### 6.1 任务目标

机械臂夹持擦拭工具，在固定区域内与平面保持接触，以稳定压力完成短距离擦拭。

该任务用于补充连续接触力控维度。相比完整擦黑板任务，它更局部、更稳定，更容易把性能差异归因到 `ForceNorm`, `ForceResultant` 和 `Depth`。

### 6.2 实验物品与初始摆放

- 使用海绵、橡皮擦、小擦布或软质擦拭块作为擦拭工具。
- 桌面或板面上标出固定擦拭区域。
- 可以使用短线段、粉笔痕迹、可擦笔痕迹或简单标记作为擦拭目标。
- 擦拭工具初始放在固定抓取区域，擦拭区域位于相机视野内。
- 擦拭表面应固定，避免受力后滑动。

### 6.3 单条 Episode 操作流程

1. 将擦拭工具放在抓取区域，将擦拭表面和目标区域固定好。
2. 控制夹爪抓取擦拭工具，确保工具不会在夹爪中旋转或脱落。
3. 移动到擦拭区域上方。
4. 缓慢下降，直到擦拭工具接触表面。
5. 建立适中压力，不要过轻离面，也不要过重卡住。
6. 沿预设短路径移动，例如直线、短弧线或小范围往返。
7. 保持接触直到完成指定路径。
8. 抬起擦拭工具并撤离。

### 6.4 成功标准

- 擦拭工具在主要运动阶段保持与表面接触。
- 完成指定短距离路径。
- 压力没有明显过大到导致卡住或拖动整个表面。
- 工具没有从夹爪中脱落。

### 6.5 失败标准

- 擦拭过程中工具离开表面。
- 压力过大导致工具卡住、表面移动或机械臂异常抖动。
- 路径明显偏离目标区域。
- 工具在夹爪中滑动、旋转或掉落。
- 擦拭动作过短，无法形成有效连续接触数据。

### 6.6 随机化设计

- 起点在固定区域内小范围变化。
- 擦拭方向可以在左到右、右到左、前到后之间变化。
- 擦拭路径长度小范围变化。
- 擦拭目标痕迹强度可小幅变化，但不要让视觉任务难度主导结果。

### 6.7 触觉机制关注点

- `ForceNorm` 用于解释法向压力是否稳定。
- `ForceResultant` 用于解释整体接触力和力矩变化。
- `Depth` 用于解释擦拭工具与表面的压缩和接触几何。
- 机制分析重点是 `force_all/depth/full` 是否比 `visual_only/rectify` 更能维持稳定接触。

### 6.8 采集注意事项

- 本任务目标不是完整擦干净整块板，而是稳定完成短距离接触擦拭。
- 不要把擦拭评价设计得过于依赖视觉痕迹清除程度。
- 优先保证压力稳定性和路径可重复性。

---

## 7. `aloha_towel_unfold`：方巾展开

### 7.1 任务目标

一只夹爪抓住方巾角点，另一只夹爪抓取或沿边缘滑动，将方巾在空中展开，使其从团聚或折叠状态变为主要边缘分离的展开状态。

该任务是高方差软体操作任务，保留为 journal scenario 或展示任务，不作为第一阶段主风险任务。

### 7.2 实验物品与初始摆放

- 使用尺寸适中的方巾或小毛巾。
- 方巾初始状态可以是轻微折叠、半展开或角点可见的团聚状态。
- 至少一个角点应在相机中可见，避免任务一开始就变成复杂搜索。
- 桌面区域应足够大，避免方巾拖到桌边或被其他物体挂住。
- 两只从臂需要有足够协作空间。

### 7.3 单条 Episode 操作流程

1. 将方巾放在预设区域，使一个角点或边缘可见。
2. 第一只夹爪接近并抓住角点。
3. 轻轻抬起方巾，使布料自然下垂，暴露边缘或第二个可抓取点。
4. 第二只夹爪接近另一角点、边缘或可滑动区域。
5. 第二只夹爪抓取边缘，或沿边缘轻轻滑动以分离折叠层。
6. 两只夹爪协调向相反方向或合适方向移动，逐步展开方巾。
7. 避免过快拉扯导致方巾掉落或缠绕。
8. 当方巾主要边缘分离、面积明显展开后保持短暂停顿。
9. 结束 episode。

### 7.4 成功标准

- 方巾从初始折叠或团聚状态变为明显展开状态。
- 至少两个主要边缘被分离。
- 方巾没有从夹爪中掉落。
- 展开结果可由相机判断。

### 7.5 失败标准

- 第一只夹爪抓错点或没有抓起方巾。
- 方巾在展开过程中掉落。
- 两只夹爪动作导致方巾缠绕更严重。
- 展开不足，最终仍然主要处于团聚状态。
- 双臂互相干涉或出现危险距离。

### 7.6 随机化设计

- 初始折叠程度小范围变化。
- 可见角点位置小范围变化。
- 方巾朝向小范围变化。
- 第一阶段不建议使用过强随机化，否则 50 条数据难以稳定训练。

### 7.7 触觉机制关注点

- `Marker2D` 可反映布料在夹爪内的滑移方向。
- `Depth` 可反映布料层叠、边缘和局部形变。
- `Force` 可反映夹持是否过松或过紧。
- 机制分析重点是 full tactile 是否能在软体操作中减少掉落和错误滑移。

### 7.8 采集注意事项

- 这是扩展展示任务，高方差较大，建议在 4 个核心任务完成后再采。
- 初始状态要有一定可重复性，否则失败原因难以归因。
- 不要把任务设计成必须完全铺平整块方巾，硕士阶段更适合评价“明显展开”和“主要边缘分离”。

---

## 8. 任务配置对照

| 任务 | 层级 | 目标 Episodes | Episode 长度 | 核心触觉机制 | 第一阶段优先级 |
| --- | --- | ---: | ---: | --- | --- |
| `aloha_flip_switch` | L1 视觉主导 | 50 | 300 frames | 负对照，触觉帮助小 | 核心 |
| `aloha_hidden_property_grasp` | L2 力控关键 | 50 | 450 frames | ForceResultant, ForceNorm, Marker2D | 核心 |
| `aloha_slip_hold_or_pull` | L2/L3 可控滑移 | 50 | 450 frames | Marker2D, Force | 核心 |
| `aloha_peg_insertion` | L3 精细触觉 | 50 | 600 frames | Marker2D, Force | 核心 |
| `aloha_pressure_wipe` | L2/L3 连续力控 | 50 | 450 frames | ForceNorm, ForceResultant, Depth | 第二轮 |
| `aloha_towel_unfold` | L4 形变操作 | 50 | 600 frames | Marker2D, Depth, Force | 扩展 |

---

## 9. 采集后检查清单

每个任务试采完成后，至少确认：

- episode 数量达到预期。
- 4 路相机视频存在且可播放。
- 4 个 XENSE 触觉数据存在。
- 单个 XENSE FULL 触觉 shape 为 `(17, 120, 160)`。
- episode 长度接近任务配置。
- 触觉可视化中能看到接触前后变化。
- 成功和失败 episode 的原因有人工记录。

如果任一检查失败，先修复采集流程或硬件状态，再继续补录。
