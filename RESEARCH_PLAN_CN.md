# 研究计划：基于多维度光子触觉感知的双臂灵巧操作研究

> 2026-05-10 | 导师：Prof. Hayashibe | 学生：Tao Liu

---

## 1. 研究动机

GelSight-style 触觉通常以接触图像/几何重建作为主要策略输入；XENSE G1-WS 光子触觉传感器除接触图像外，还可显式采集 **7 类共 17 通道的物理状态**——包括三维力分布、深度图、滑移检测和六维合力。这引出一个核心问题：

> 哪些触觉维度对哪些任务有贡献？能否系统性地理解"新增物理状态在何时、以何种机制提升操作性能"？

导师的核心思想：**人类用"动态卷积"调节感知分辨率**——简单任务用大卷积核粗略感知，精细任务用小卷积核精密处理。类似地，机器人应该**根据任务自评估所需触觉分辨率**，而非始终使用全部触觉信息。

---

## 2. 研究问题

1. **传感器对比**：GelSight-style image tactile vs XENSE SIMPLE（图像触觉 baseline）vs XENSE FULL（显式物理状态），量化每个新增维度的边际贡献
2. **任务-触觉映射**：哪些任务触觉信息可有可无？哪些任务触觉信息是关键？
3. **机制解释**：不止报性能数字，要分析为什么特定通道对特定任务有效（如 Marker2D 在插入任务中检测微滑移）
4. **动态触觉分辨率**（后续）：机器人能否学会自评估当前任务需要多少触觉精度？

---

## 3. 硬件平台


| 组件    | 详情                                             |
| ----- | ---------------------------------------------- |
| 机械臂   | ALOHA2 四臂：2× WidowX 250（主臂）+ 2× ViperX-300（从臂） |
| 相机    | 4× Intel RealSense（640×480, 30 FPS）            |
| 触觉传感器 | 4× XENSE G1-WS 光子触觉传感器，安装在从臂夹爪                 |


### 触觉传感器 FULL 模式通道（17 通道/传感器）


| 通道             | 维数  | 物理含义               | 解决什么问题              |
| -------------- | --- | ------------------ | ------------------- |
| Rectify        | 1   | 校正灰度接触图像           | 接触面视觉纹理（类 GelSight） |
| Difference     | 1   | 帧间差分               | 检测瞬时接触变化            |
| Depth          | 1   | 深度图（mm）            | 接触面三维形貌             |
| Force          | 3   | 三维力分布 (Fx, Fy, Fz) | 每个局部点受力大小和方向        |
| ForceNorm      | 3   | 法向力分量              | 正压力分布               |
| ForceResultant | 6   | 六维合力/力矩            | 总接触力+力矩，判断夹持稳定性     |
| Marker2D       | 2   | 切向位移场              | 直接检测物体是否滑移及方向       |


### GelSight-style vs XENSE 对比


| 维度                | GelSight-style image tactile | XENSE SIMPLE                | XENSE FULL                           |
| ----------------- | ---------------------------- | --------------------------- | ------------------------------------ |
| 策略输入              | 接触图像/几何重建为主                  | Rectify 灰度接触图像              | Rectify + 显式物理状态                     |
| 接触图像              | ✅ 高分辨率图像触觉                   | ✅ 灰度                        | ✅ 校正灰度                               |
| 深度/几何             | ✅ 可由图像重建/估计                  | ❌                           | ✅ 原生 Depth                           |
| 力相关信息             | 可估计，取决于模型/标定                 | ❌                           | ✅ Force + ForceNorm + ForceResultant |
| 滑移/切向位移           | 可由图像/marker 估计               | ❌                           | ✅ Marker2D 直接输出                      |
| 本研究中的 baseline 含义 | image-only tactile baseline  | image-only tactile baseline | explicit physical-state tactile      |


这就是导师所说的 **"additional physical states"**——本研究不把 GelSight 简化为“没有 3D/力信息”，而是将 `rectify`/SIMPLE 定义为 **image-only tactile baseline**，再比较 XENSE FULL 中显式输出的力、深度、滑移等物理量对哪些任务有帮助、为什么。

---

## 4. 任务设计

任务设计从“复杂展示任务”调整为“任务-触觉维度解耦”：第一轮 4 个核心任务，每个任务突出一种主要触觉机制；第二轮再加入压力擦拭和布料展开扩展任务。

### L1：`aloha_flip_switch`（拨动开关）— 视觉主导 / 负对照


| 参数     | 值                 |
| ------ | ----------------- |
| 目标     | 拨动大型拨杆开关（上/下）     |
| 采集量    | 50 episodes × 10s |
| 核心触觉通道 | Rectify（预期帮助小）    |


**科学角色**：证明**触觉并非总是必要**。刚性接触、视觉定位即可完成任务。预期：`visual_only ≈ tactile_full`。

### L2：`aloha_hidden_property_grasp`（隐藏属性抓取）— 核心亮点任务


| 参数     | 值                                   |
| ------ | ----------------------------------- |
| 目标     | 抓起外观相同但重量/软硬/摩擦不同的物体，放入碗中，不掉落       |
| 采集量    | 50 episodes × 15s                   |
| 核心触觉通道 | ForceResultant, ForceNorm, Marker2D |


**科学角色**：视觉传感器无法区分外观相同物体的重量、软硬度、表面摩擦系数。**触觉是唯一能检测这些隐藏属性的模态。** 这是证明触觉必要性的最强证据。

实验设计要点：

- 准备 3~4 组外观相同但属性不同的物体对（如：同色方块，但重量 50g vs 200g）
- 随机化物体出现顺序，确保策略不能靠记忆
- 成功标准：抓取、搬运、放入碗中，不掉落

### L2/L3：`aloha_slip_hold_or_pull`（滑移保持/拉动）— Marker2D 机制任务


| 参数     | 值                                   |
| ------ | ----------------------------------- |
| 目标     | 夹住布条/纸片/橡胶片或小物体，在可控滑移中保持不掉落或按指定距离拉动 |
| 采集量    | 50 episodes × 15s                   |
| 核心触觉通道 | Marker2D, Force                     |


**科学角色**：直接验证滑移检测机制。相比完整 `towel_unfold`，该任务减少布料初始状态、双臂协调和展开评价带来的高方差，更清楚地检验 Marker2D 是否能帮助策略发现滑移并调整夹持。

### L3：`aloha_peg_insertion`（轴孔插入）— 精细触觉 Benchmark


| 参数     | 值                    |
| ------ | -------------------- |
| 目标     | 将 USB 插头或销钉插入紧密配合的孔位 |
| 采集量    | 50 episodes × 20s    |
| 核心触觉通道 | Marker2D, Force      |


**科学角色**：经典操作 Benchmark。Marker2D 能直接输出插入过程中的微滑移和错位信号；相比 image-only tactile baseline，策略不必再从接触图像中间接估计该物理状态。Force 检测接触力不对称以判断是否卡住。

### L2/L3（第二轮）：`aloha_pressure_wipe`（固定区域短距离擦拭）— 连续接触力控


| 参数     | 值                                |
| ------ | -------------------------------- |
| 目标     | 在固定区域内保持接触并完成短距离擦拭，不追求整块板擦净度     |
| 采集量    | 50 episodes × 15s                |
| 核心触觉通道 | ForceNorm, ForceResultant, Depth |


**科学角色**：补充连续力控维度。相比原 `erase_board_pressure`，评价目标更局部、更稳定，便于把性能差异归因到 ForceNorm / ForceResultant / Depth。

### L4（扩展展示）：`aloha_towel_unfold`（方巾展开）— 高方差软体操作


| 参数     | 值                           |
| ------ | --------------------------- |
| 目标     | 一只夹爪拎起方巾一角，另一只夹爪沿边缘滑动并在空中展开 |
| 采集量    | 可选 50 episodes × 20s        |
| 核心触觉通道 | Marker2D, Depth, Force      |


**科学角色**：保留为 journal scenario 或展示任务。它能体现软体操作价值，但布料初始状态、抓取点、双臂协调和展开评价都会引入高方差，因此不作为硕士第一阶段核心任务。

---

## 5. 消融实验矩阵

每个任务最多训练 7 个策略变体；硕士第一轮优先训练 4 个核心任务 × 4 个核心变体，时间允许再扩展到 6 个候选任务 × 7 个变体：


| 变体            | 触觉通道                               | 通道数 | 含义                 |
| ------------- | ---------------------------------- | --- | ------------------ |
| `visual_only` | 无                                  | 0   | 仅视觉 baseline       |
| `rectify`     | Rectify                            | 1   | GelSight 等效（仅接触图像） |
| `force`       | Force                              | 3   | 三维局部力分布            |
| `force_all`   | Force + ForceNorm + ForceResultant | 12  | 所有力相关通道            |
| `depth`       | Depth                              | 1   | 仅深度图               |
| `marker`      | Marker2D                           | 2   | 仅滑移检测              |
| `full`        | 全部 7 类                             | 17  | 完整触觉信息             |


### 分析维度

1. **任务内对比**：每个任务中各变体相比 baseline 提升了多少？
2. **通道贡献归因**：哪些通道对哪些任务驱动力最大？
3. **层级梯度验证**：触觉重要性是否从 L1→L4 递增？
4. **通道独立 vs 协同**：多通道组合是否产生超加性效果？

---

## 6. 预期实验结果

### 假设结果矩阵（★ = 成功率）


| 任务                        | visual_only | rectify | force | force_all | depth | marker | full |
| ------------------------- | ----------- | ------- | ----- | --------- | ----- | ------ | ---- |
| flip_switch (L1)          | ★★★         | ★★★     | ★★★   | ★★★       | ★★★   | ★★★    | ★★★  |
| hidden_property (L2)      | ★           | ★       | ★★    | ★★★       | ★★    | ★★     | ★★★  |
| slip_hold_or_pull (L2/L3) | ★★          | ★★      | ★★    | ★★        | ★     | ★★★    | ★★★  |
| peg_insertion (L3)        | ★           | ★★      | ★★    | ★★        | ★     | ★★★    | ★★★  |
| pressure_wipe (optional)  | ★★          | ★★      | ★★    | ★★★       | ★★★   | ★★     | ★★★  |


### 核心叙事

> "触觉感知的价值不是一刀切的——它的必要性取决于任务性质。隐藏物理属性、可控滑移、精细插入和连续按压力控制分别依赖不同触觉维度。我们的消融实验揭示了**哪些维度在哪些任务中起作用**，为任务驱动的触觉传感器设计和动态触觉分辨率分配提供依据。"

---

## 7. 第二部分（硕士论文第二贡献）：轻量级动态触觉分辨率

基于第一部分消融实验的发现，第二部分先做可落地的 **Adaptive Tactile Resolution** 最小原型：机器人在不同任务中自评估需要低、中、高哪一级触觉输入。完整 MAML 触觉元学习作为后续扩展，不作为当前硕士主风险。

### 核心思想：触觉"动态卷积"

人类观看 4K 电视时并非全分辨率处理画面——注意力集中时用小卷积核精细感知，随意浏览时用大卷积核粗略扫描。这是**计算高效**的策略：处理精度根据任务需求自适应调整。

同样地，触觉感知也应当动态调整分辨率：

- **精细操作**（轴孔插入、穿线）→ 小卷积核、高触觉分辨率、激活全部物理状态通道
- **粗糙操作**（拨动开关、推方块）→ 大卷积核、低触觉分辨率、仅需单通道
- **中间任务**→ 自适应分辨率，部分通道激活

### 形式化定义

```
触觉处理 = f(触觉输入, σ(任务上下文))

其中 σ 为分辨率控制器（Resolution Controller）：
  - kernel_size ∈ {3, 5, 7, 9}       （小核 = 精细感知，大核 = 粗略扫描）
  - channel_group ∈ {SIMPLE仅, task-relevant, 全通道}
  - spatial_downsample ∈ {1×, 2×, 4×}
```

### 推荐架构：Gated Tactile Encoder（门控触觉编码器）

```
┌─────────────────┐
│  任务上下文      │  ← 视觉特征 + 机械臂本体感知
│  (视觉+状态)     │
└────────┬─────────┘
         │
┌────────▼─────────┐
│  分辨率控制器    │  ← Gumbel-Softmax 门控（可微分离散选择）
│  (Resolution     │    学习输出 (kernel_size, channel_mask, downsample_ratio)
│   Controller)    │
└────────┬─────────┘
         │
┌────────▼─────────┐
│  动态触觉编码器  │  ← 根据控制器输出自适应调整卷积核大小与通道数
│  (Dynamic        │
│   Tactile Encoder)│
└────────┬─────────┘
         │
┌────────▼─────────┐
│  策略头          │  ← ACT / Diffusion 动作预测
│  (Policy Head)   │
└──────────────────┘
```

### 训练目标

```
L_total = L_task + λ × FLOPs(分辨率选择) / FLOPs_max
```

通过计算量正则化项，**激励模型在简单任务时自动选择低成本触觉处理，仅在必要时激活高分辨率**——实现"自评估驱动的高效计算"。

### 自评估机制（Self-Evaluation）

核心创新：**机器人自己判断当前任务需要什么触觉分辨率。**

实现路径：

1. **低成本路径**：`rectify` / SIMPLE，适合 `flip_switch` 等触觉需求低的任务
2. **中等路径**：任务相关通道组，如 `force_all` 或 `marker2d`
3. **高成本路径**：`full` 17 通道，适合隐藏属性、插入、软体操作等任务
4. **门控选择器**：根据视觉特征、机器人状态和早期接触帧选择路径，并用计算量/延迟正则化鼓励简单任务走低成本路径

### 预期贡献

1. **任务依赖的触觉物理状态价值分析**：回答哪些任务需要哪些触觉状态
2. **计算高效**：机器人根据任务需求按需分配触觉计算资源
3. **自感知能力**：机器人学会评估自身感知需求
4. **可扩展到元学习**：MAML / few-shot tactile adaptation 作为后续博士或期刊扩展，而非当前硕士主风险

### 对导师核心问题的直接回应

1. **"人眼看电视"的类比** → Gated Tactile Encoder 通过可学习的分辨率控制器实现"需要精细时小核、需要粗略时大核"
2. **"任务自评估"** → 门控选择器自行判断触觉分辨率需求
3. **"计算高效"** → FLOPs 正则化激励模型选择低成本处理路径
4. **"可能的新研究范式"** → 触觉元学习保留为后续扩展，当前先验证最小可行的自评估机制

---

## 8. 实施路线图

### Phase 1：数据采集（进行中）

- 第一轮录制 4 个核心任务 × 50 episodes = 200 episodes
- 第二轮按时间补充 `aloha_pressure_wipe` 和 `aloha_towel_unfold`
- FULL 模式 17 通道 + 4 路相机
- 预计 ~8 小时遥操作

### Phase 2：消融训练

- 优先训练 4 个核心任务：`flip_switch`、`hidden_property_grasp`、`slip_hold_or_pull`、`peg_insertion`
- 每个任务先跑 4 个核心变体：`visual_only`、`rectify`、任务相关最佳通道组、`full`
- 时间允许再扩展到 6 任务 × 7 变体完整矩阵
- 评估各变体成功率
- 可视化触觉注意力图

### Phase 3：分析与论文写作

- 逐通道贡献分析
- 机制分析：slip/contact onset、ForceResultant 抓取稳定性、Depth 接触几何、`rectify ≈ full` vs `rectify << full` 的任务边界
- 失败模式定性分析
- 撰写论文

### Phase 4（第二贡献原型）：动态触觉分辨率

- Gated Tactile Encoder + 可学习的 Resolution Controller
- 任务自评估模块
- 三路径选择：low/simple、medium/task-relevant、full
- MAML-based 触觉适应仅作为后续扩展

---

## 8. 代码路径


| 功能     | 路径                                              |
| ------ | ----------------------------------------------- |
| 数据录制   | `examples/aloha_ros/aloha_ros_record.py`        |
| 模型训练   | `examples/aloha2/train_aloha2_tactile.py`       |
| 触觉可视化  | `examples/aloha2/extract_tactile_viz.py`        |
| 任务配置   | `examples/aloha_ros/aloha_scripts/constants.py` |
| 触觉驱动   | `src/lerobot/tactile/xense_g1ws/`               |
| ACT 模型 | `src/lerobot/policies/act/`                     |


技术文档：`SETUP_SUMMARY.md`
进度追踪：`PROGRESS.md`
英文版：`RESEARCH_PLAN.md`